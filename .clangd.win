# https://github.com/clangd/clangd/discussions/1984
If:                                            # Apply this config conditionally
  PathMatch: [source/.*, examples/.*, tests/.*] # our C++ under source/, examples/, tests/
  PathExclude: external/.*                       # but not third-party under external/
CompileFlags:
  Add: ['-std:c++latest']